[workspace]
members = [
    "cloud_sync_lib",
    "cloud_sync_daemon",
    "cloud_sync_ui",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

